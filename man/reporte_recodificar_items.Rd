% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicador_recodificacion.R
\name{reporte_recodificar_items}
\alias{reporte_recodificar_items}
\title{Recodificar items categóricos a escala estandarizada 0-100}
\usage{
reporte_recodificar_items(
  data,
  instrumento = NULL,
  vars = NULL,
  excluir_vars = NULL,
  orden_por_lista = NULL,
  codigos_missing = character(0),
  codigos_no_aplica = character(0),
  prefijo = "r100_",
  reemplazar = FALSE
)
}
\arguments{
\item{data}{Objeto devuelto por \code{\link[=reporte_data]{reporte_data()}} (clase
\code{"prosecnur_reporte_tbl"}) o un \code{data.frame} con estructura equivalente.}

\item{instrumento}{Objeto devuelto por \code{\link[=reporte_instrumento]{reporte_instrumento()}}. Si es \code{NULL},
se toma desde \code{attr(data, "instrumento_reporte")}.}

\item{vars}{Variables a recodificar. Si es \code{NULL}, usa todas las \code{select_one}
del instrumento presentes en \code{data}.}

\item{excluir_vars}{Variables a excluir de la recodificación.}

\item{orden_por_lista}{Configuración de orden ascendente (de menor a mayor)
por \code{list_name}, usando códigos (\code{choices$name}). Debe ser una lista
nombrada por \code{list_name}, donde cada elemento es un vector de códigos en
el orden deseado.}

\item{codigos_missing}{Códigos a tratar como \code{missing}, usando solo códigos
(\code{choices$name}). Acepta vector global de códigos o lista nombrada por
\code{list_name} (opcionalmente con \code{.default}).}

\item{codigos_no_aplica}{Códigos a tratar como \code{no_aplica}, usando solo
códigos (\code{choices$name}). Acepta vector global de códigos o lista nombrada
por \code{list_name} (opcionalmente con \code{.default}).}

\item{prefijo}{Prefijo para nuevas columnas recodificadas cuando
\code{reemplazar = FALSE}.}

\item{reemplazar}{Si \code{TRUE}, reemplaza la variable original. Si \code{FALSE},
crea columnas nuevas con prefijo.}
}
\value{
El mismo \code{data.frame} con variables recodificadas a 0-100. Además
agrega atributo \code{recodificacion_items_meta} con trazabilidad por variable
(list_name, mapeo aplicado y conteos de calidad).
}
\description{
Toma una base procesada con \code{\link[=reporte_data]{reporte_data()}} y recodifica variables
\code{select_one} usando la estructura del instrumento de \code{\link[=reporte_instrumento]{reporte_instrumento()}}.
La recodificación se hace por orden de categorías del \code{list_name} y convierte
respuestas sustantivas a una escala 0-100.
}
\details{
Reglas para categorías especiales:
\itemize{
\item \code{codigos_missing} se tratan como \code{missing}.
\item \code{codigos_no_aplica} se tratan como \code{no_aplica}.
\item Tanto \code{missing} como \code{no_aplica} se recodifican a \code{NA_real_}.
}
}
\seealso{
Other indicador: 
\code{\link{indicador}()},
\code{\link{nivel}()},
\code{\link{reporte_construir_indices}()},
\code{\link{reporte_dimensiones_config}()},
\code{\link{reporte_indicadores}()},
\code{\link{reporte_tablas_recodificadas}()}
}
\concept{indicador}
