% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generador_ppt.R
\name{reporte_ppt}
\alias{reporte_ppt}
\title{Crear un reporte en PowerPoint a partir de resultados de encuesta}
\usage{
reporte_ppt(
  data,
  instrumento = NULL,
  secciones = NULL,
  path_ppt = "reporte_ppt.pptx",
  fuente = NULL,
  sm_vars_force = NULL,
  mostrar_todo = FALSE,
  solo_lista = FALSE,
  incluir_titulo_var = TRUE,
  mensajes_progreso = TRUE,
  bloques_multi_apiladas = NULL,
  pares_diapositiva = NULL,
  vars_dico = NULL,
  vars_barras_apiladas = NULL,
  vars_barras_agrupadas = NULL,
  vars_radar = NULL,
  listnames_dico = NULL,
  listnames_apiladas = NULL,
  listnames_barras_agrupadas = NULL,
  dico_labels_por_var = list(),
  dico_labels_por_listname = list(),
  colores_apiladas_por_listname = list(),
  default_so = c("barras_apiladas", "barras_agrupadas"),
  default_sm = c("barras_agrupadas", "barras_apiladas"),
  barra_extra = c("ninguna", "total_n"),
  estilos_barras_agrupadas = list(),
  estilos_barras_apiladas = list(),
  estilos_dico = list(),
  template_pptx = NULL,
  titulo_portada = NULL,
  subtitulo_portada = NULL,
  fecha_portada = NULL,
  mostrar_resumen_n = TRUE,
  debug_ph_bordes = FALSE
)
}
\arguments{
\item{data}{\code{data.frame} o \code{tibble} con las variables (o sus dummies) a reportar.}

\item{instrumento}{Objeto de instrumento con al menos \code{survey} (y opcionalmente \code{choices}
y \code{orders_list}). Si es \code{NULL}, se busca el atributo \code{instrumento_reporte} en \code{data}.}

\item{secciones}{Lista nombrada donde cada elemento contiene un vector de nombres de variables
a incluir por sección. Si es \code{NULL}, se intenta inferir desde \code{survey$section} o \code{survey$seccion}.}

\item{path_ppt}{Ruta del archivo \code{.pptx} de salida.}

\item{fuente}{Texto opcional que se agrega como fuente/base en las diapositivas.}

\item{sm_vars_force}{Vector de variables que deben tratarse como selección múltiple (si aplica
en la lógica de clasificación).}

\item{mostrar_todo}{Si \code{TRUE}, conserva opciones con frecuencia cero al construir tablas (si la
tabla de frecuencias lo permite).}

\item{solo_lista}{Si \code{TRUE}, no se genera el PowerPoint. Se devuelven gráficos y el log.}

\item{incluir_titulo_var}{Si \code{TRUE}, usa el título de la pregunta como título de la diapositiva.}

\item{mensajes_progreso}{Si \code{TRUE}, imprime mensajes de avance durante el proceso.}

\item{bloques_multi_apiladas}{Lista nombrada de bloques especiales para apiladas múltiples.
Cada bloque debe incluir al menos \code{vars} (vector de variables) y puede incluir \code{titulo}
y \code{wrap_y}.}

\item{pares_diapositiva}{Lista para definir pares de gráficos en una misma diapositiva.
Cada elemento debe incluir \code{vars} (vector de longitud 2) y opcionalmente \code{titulo}.}

\item{vars_dico, vars_barras_apiladas, vars_barras_agrupadas, vars_radar}{Vectores de nombres de
variables que fuerzan el tipo de gráfico por variable.}

\item{listnames_dico, listnames_apiladas}{Vectores de \code{list_name} que fuerzan el tipo de gráfico
por lista.}

\item{dico_labels_por_var}{Lista con etiquetas dicotómicas por variable. Cada elemento debe
ser un vector de dos etiquetas (por ejemplo, \code{c("Sí", "No")}).}

\item{dico_labels_por_listname}{Lista con etiquetas dicotómicas por \code{list_name}, con el mismo
formato que \code{dico_labels_por_var}.}

\item{colores_apiladas_por_listname}{Lista con definiciones de colores para barras apiladas
por \code{list_name}. Puede incluir un vector nombrado de colores y, opcionalmente, parámetros
extra asociados al estilo de barra adicional.}

\item{default_so}{Tipo de gráfico por defecto para variables de opción única (por ejemplo,
\code{"barras_apiladas"} o \code{"barras_agrupadas"}).}

\item{default_sm}{Tipo de gráfico por defecto para variables de selección múltiple.}

\item{barra_extra}{Define el tratamiento de una anotación adicional en gráficos de barras:
\code{"ninguna"} o \code{"total_n"}.}

\item{estilos_barras_agrupadas}{Lista de parámetros de estilo que se pasan al graficador de
barras agrupadas.}

\item{estilos_barras_apiladas}{Lista de parámetros de estilo que se pasan al graficador de
barras apiladas.}

\item{estilos_dico}{Lista de parámetros de estilo que se pasan al graficador dicotómico.}

\item{template_pptx}{Ruta a una plantilla \code{.pptx} para definir layouts y estilos. Si es \code{NULL},
se intenta usar una plantilla interna y, en su defecto, la plantilla por defecto de PowerPoint.}

\item{titulo_portada, subtitulo_portada, fecha_portada}{Textos opcionales para incluir una diapositiva
de portada si la plantilla cuenta con un layout compatible.}

\item{mostrar_resumen_n}{Si \code{TRUE}, considera incluir un resumen de N (según el layout disponible).}

\item{debug_ph_bordes}{Si \code{TRUE}, activa bordes de depuración en los gráficos que usan canvas
(útil para revisar alineaciones internas).}
}
\value{
Devuelve invisiblemente una lista con:
\describe{
\item{plots}{Lista de gráficos generados (en el mismo orden de salida).}
\item{log_decisiones}{Tabla con las decisiones tomadas por variable (tipo, override, \code{list_name}, etc.).}
}
Si \code{solo_lista = TRUE}, el objeto retornado contiene los gráficos y el log, pero no se escribe el PPT.
}
\description{
Genera un archivo \strong{.pptx} con una diapositiva por variable (y, de forma opcional,
algunas diapositivas con \strong{dos gráficos}). El flujo general es:
(1) identificar las variables a reportar (desde \code{secciones} o desde el instrumento),
(2) calcular tablas de frecuencias,
(3) elegir el tipo de gráfico según reglas y prioridades (por variable, por \code{list_name}
o por defecto), y (4) armar el PowerPoint usando una plantilla.
}
\details{
El tipo de gráfico se selecciona con un orden de preferencia:
primero sobrescrituras explícitas (\verb{vars_*} y \verb{listnames_*}), y si no hay coincidencias,
se aplican los valores por defecto (\code{default_so} y \code{default_sm}) según el tipo de pregunta.
Además, se admite un modo especial para construir \strong{bloques de varias preguntas} en una
sola visualización apilada (\code{bloques_multi_apiladas}).

El PowerPoint puede generarse desde una plantilla propia (\code{template_pptx}) o, si está
disponible, desde la plantilla incluida en el paquete. Si \code{solo_lista = TRUE}, la función
no escribe el archivo y únicamente devuelve los gráficos y el registro de decisiones.
}
\examples{
\dontrun{
out <- reporte_ppt(
  data = rp_data,
  instrumento = rp_inst,
  secciones = list("Sección 1" = c("p1", "p2")),
  path_ppt = "reporte.pptx",
  fuente = "Pulso PUCP"
)
}

}
