% Generated by hand (compatible with roxygen style)
\name{reporte_construir_indices}
\alias{reporte_construir_indices}
\title{Construir índices jerárquicos desde variables recodificadas (0-100)}
\usage{
reporte_construir_indices(
  data,
  bloques,
  indices = NULL,
  min_prop_valid = 0.5,
  prefijo_bloque = "bloq_",
  prefijo_indice = "idx_"
)
}
\arguments{
\item{data}{\code{data.frame} con ítems recodificados (0-100).}

\item{bloques}{Lista nombrada de primer nivel. Cada elemento es un vector de
variables a promediar.}

\item{indices}{Lista nombrada opcional de segundo nivel. Cada elemento puede
referir nombres de bloque o variables existentes en \code{data}.}

\item{min_prop_valid}{Proporción mínima de ítems válidos requerida por fila.
Puede ser escalar o vector nombrado (opcionalmente con \code{.default}).}

\item{prefijo_bloque}{Prefijo de columnas creadas para bloques.}

\item{prefijo_indice}{Prefijo de columnas creadas para índices.}
}
\value{
El mismo \code{data.frame} con columnas nuevas para bloques e índices. Agrega
atributo \code{indices_meta} con trazabilidad de cálculo.
}
\description{
Calcula agregaciones jerárquicas por promedio simple de forma declarativa,
sin asumir nombres específicos de estudio.
}
