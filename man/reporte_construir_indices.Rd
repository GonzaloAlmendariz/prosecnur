% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicador_recodificacion.R
\name{reporte_construir_indices}
\alias{reporte_construir_indices}
\title{Construir índices jerárquicos desde variables recodificadas (0-100)}
\usage{
reporte_construir_indices(
  data,
  bloques,
  indices = NULL,
  min_prop_valid = 0.5,
  prefijo_bloque = "bloq_",
  prefijo_indice = "idx_"
)
}
\arguments{
\item{data}{\code{data.frame} con ítems recodificados (0-100).}

\item{bloques}{Lista nombrada. Cada elemento es un vector de variables que
componen un bloque de primer nivel.}

\item{indices}{Lista nombrada opcional. Cada elemento es un vector de
referencias para construir índices de segundo nivel. Puede referir a:
\itemize{
\item nombres de bloque (clave en \code{bloques}), o
\item variables/columnas ya existentes en \code{data}.
}}

\item{min_prop_valid}{Proporción mínima de ítems válidos requerida por fila.
Puede ser un número único (ej. \code{0.5}) o un vector nombrado por bloque/índice
(opcionalmente con \code{.default}).}

\item{prefijo_bloque}{Prefijo de columnas creadas para bloques.}

\item{prefijo_indice}{Prefijo de columnas creadas para índices.}
}
\value{
El mismo \code{data.frame} con columnas agregadas para bloques e índices.
Agrega además \code{attr(x, "indices_meta")} con trazabilidad de cálculos.
}
\description{
Calcula promedios simples por bloques (por ejemplo, conductores) y luego
índices de segundo nivel a partir de esos bloques u otras variables numéricas.
La función es agnóstica al estudio: solo usa las declaraciones entregadas.
}
\seealso{
Other indicador: 
\code{\link{indicador}()},
\code{\link{nivel}()},
\code{\link{reporte_dimensiones_config}()},
\code{\link{reporte_indicadores}()},
\code{\link{reporte_recodificar_items}()},
\code{\link{reporte_tablas_recodificadas}()}
}
\concept{indicador}
