% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graficar_radar.R
\name{graficar_radar}
\alias{graficar_radar}
\title{Graficar un radar (telaraña) para varias dimensiones e indicadores}
\usage{
graficar_radar(
  data,
  var_categoria,
  cols_valor,
  etiquetas_series,
  escala_valor = c("proporcion_1", "proporcion_100"),
  colores_series = NULL,
  breaks_radiales = c(0.2, 0.4, 0.6, 0.8, 1),
  mostrar_radios = TRUE,
  mostrar_rejilla = TRUE,
  mostrar_labels_radiales = TRUE,
  dist_etiquetas_rel = 1.12,
  alpha_relleno = 0.2,
  mostrar_puntos = TRUE,
  mostrar_valores = FALSE,
  decimales = 1,
  color_texto_valores = "#000000",
  size_texto_valores = 3,
  titulo = NULL,
  subtitulo = NULL,
  nota_pie = NULL,
  color_titulo = "#000000",
  size_titulo = 11,
  color_subtitulo = "#000000",
  size_subtitulo = 9,
  color_nota_pie = "#000000",
  size_nota_pie = 8,
  color_leyenda = "#000000",
  size_leyenda = 8,
  color_texto_breaks = "#666666",
  size_texto_breaks = 3,
  size_dimensiones = 4,
  color_dimensiones = "#000000",
  mostrar_leyenda = TRUE,
  invertir_leyenda = FALSE,
  textos_negrita = NULL,
  exportar = c("rplot", "png", "ppt"),
  path_salida = NULL,
  ancho = 8,
  alto = 8,
  dpi = 300
)
}
\arguments{
\item{data}{Data frame en formato ancho que contiene la columna de
dimensiones y las columnas de valores.}

\item{var_categoria}{Nombre (string) de la columna que define la dimensión
(por ejemplo, \code{"dominio"} o \code{"dimension"}).}

\item{cols_valor}{Vector de nombres de columnas numéricas con los valores
a comparar (por ejemplo, \code{c("pct_calidad","pct_acceso","pct_satisf")}).}

\item{etiquetas_series}{Vector de caracteres con nombre (named) que asigna
etiquetas legibles a las columnas de \code{cols_valor}. Los nombres deben
coincidir exactamente con \code{cols_valor}; los valores se usan en la
leyenda.}

\item{escala_valor}{Escala de los valores: \code{"proporcion_1"} si están
en 0–1, o \code{"proporcion_100"} si están en 0–100.}

\item{colores_series}{Vector de colores HEX con nombre (opcional) para las
series, usando como nombres las etiquetas de \code{etiquetas_series}.
Si se omite, \pkg{ggplot2} asignará colores por defecto.}

\item{breaks_radiales}{Vector numérico con los valores radiales (0–1) para
las circunferencias de referencia (rejilla).}

\item{mostrar_radios}{Lógico; si \code{TRUE}, muestra las líneas rectas que
van del centro hacia las dimensiones (ejes radiales).}

\item{mostrar_rejilla}{Lógico; si \code{TRUE}, muestra las circunferencias
(poligonales) de referencia en los \code{breaks_radiales}.}

\item{mostrar_labels_radiales}{Lógico; si \code{TRUE}, muestra las etiquetas
de texto de los niveles radiales (por ejemplo, "20\%", "40\%", etc.).}

\item{dist_etiquetas_rel}{Factor multiplicativo (>1) que controla qué tan
lejos del polígono se ubican las etiquetas de las dimensiones.}

\item{alpha_relleno}{Nivel de transparencia (0–1) para el relleno de los
polígonos de cada serie.}

\item{mostrar_puntos}{Lógico; si \code{TRUE}, dibuja puntos en los vértices.}

\item{mostrar_valores}{Lógico; si \code{TRUE}, muestra el valor (porcentaje)
cerca de cada vértice.}

\item{decimales}{Número de decimales para los valores mostrados cuando
\code{mostrar_valores = TRUE}.}

\item{color_texto_valores}{Color del texto de los valores.}

\item{size_texto_valores}{Tamaño del texto de los valores.}

\item{titulo}{Título del gráfico.}

\item{subtitulo}{Subtítulo opcional.}

\item{nota_pie}{Nota o fuente para el pie de página (alineado a la derecha).}

\item{color_titulo}{Color del título.}

\item{size_titulo}{Tamaño del título.}

\item{color_subtitulo}{Color del subtítulo.}

\item{size_subtitulo}{Tamaño del subtítulo.}

\item{color_nota_pie}{Color del texto del pie de página.}

\item{size_nota_pie}{Tamaño del texto del pie de página.}

\item{color_leyenda}{Color del texto de la leyenda.}

\item{size_leyenda}{Tamaño del texto de la leyenda.}

\item{color_texto_breaks}{Color del texto de los breaks radiales.}

\item{size_texto_breaks}{Tamaño del texto de los breaks radiales.}

\item{size_dimensiones}{Tamaño del texto de cada dimensión alrededor
del radar.}

\item{color_dimensiones}{Color del texto de cada dimensión.}

\item{mostrar_leyenda}{Lógico; si \code{FALSE}, oculta la leyenda.}

\item{invertir_leyenda}{Lógico; si \code{TRUE}, invierte el orden de los
ítems de la leyenda.}

\item{textos_negrita}{Vector de caracteres que indica qué elementos deben
mostrarse en negrita. Puede incluir cualquiera de:
\code{"titulo"}, \code{"leyenda"}, \code{"valores"}, \code{"dimensiones"}.}

\item{exportar}{Método de salida: \code{"rplot"} (devuelve un objeto
\code{ggplot}), \code{"png"} (exporta un archivo PNG) o \code{"ppt"}
(exporta una diapositiva PPTX con el gráfico incrustado).}

\item{path_salida}{Ruta del archivo a crear cuando \code{exportar} no es
\code{"rplot"}.}

\item{ancho}{Ancho del gráfico (cuando se exporta a archivo).}

\item{alto}{Alto del gráfico (cuando se exporta a archivo).}

\item{dpi}{Resolución en puntos por pulgada (solo para PNG).}
}
\value{
Un objeto \code{ggplot} si \code{exportar = "rplot"}. De forma
invisible, el gráfico exportado si se utiliza \code{"png"} o \code{"ppt"}.
}
\description{
Genera un gráfico tipo radar (telaraña) donde cada dimensión
(por ejemplo, "Calidad de atención", "Accesibilidad") se representa como
un eje alrededor de una figura poligonal, y para cada una se comparan dos
o más series (por ejemplo, "Calidad percibida", "Acceso", "Satisfacción").
}
\details{
Los datos se esperan en formato ancho: una columna categórica que define
las dimensiones y varias columnas numéricas con porcentajes o puntajes
a comparar.
}
