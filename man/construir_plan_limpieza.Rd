% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/construir_plan.R
\name{construir_plan_limpieza}
\alias{construir_plan_limpieza}
\title{Construir plan de limpieza desde un XLSForm}
\usage{
construir_plan_limpieza(
  ruta_xlsform,
  idioma = "es",
  config = acnur_config(),
  incluir = list(requeridos = TRUE, otros = TRUE, relevantes = TRUE, restricciones =
    TRUE, calculos = TRUE, controles = TRUE, constantes = TRUE, atipicos = TRUE),
  ruta_salida = NULL,
  silencioso = FALSE
)
}
\arguments{
\item{ruta_xlsform}{Ruta al archivo XLSForm (.xlsx).}

\item{idioma}{Código de idioma para labels (ej. "es").}

\item{config}{Lista de configuración (por defecto acnur_config()).}

\item{incluir}{Lista de banderas para incluir bloques de reglas:
list(requeridos=TRUE, otros=TRUE, relevantes=TRUE, restricciones=TRUE,
calculos=TRUE, controles=TRUE, constantes=TRUE, atipicos=TRUE)}

\item{ruta_salida}{(opcional) Ruta para exportar a Excel. Si NULL, no exporta.}

\item{silencioso}{Si TRUE, reduce mensajes informativos.}
}
\value{
Lista con:
- plan: tibble con el plan
- resumen: tibble con conteo por tipo de observación
- secciones: mapa de secciones/prefijos
- meta: metadata del XLSForm
}
\description{
Construir plan de limpieza desde un XLSForm
}
