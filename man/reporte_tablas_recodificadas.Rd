% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicador_recodificacion.R
\name{reporte_tablas_recodificadas}
\alias{reporte_tablas_recodificadas}
\title{Exportar tablas SPSS de promedios (0-100) con cruces horizontales}
\usage{
reporte_tablas_recodificadas(
  data,
  secciones,
  tablas = NULL,
  titulo_default = "INDICES",
  modo_cruces = c("juntos", "separados"),
  separacion_filas = 3,
  fila = NULL,
  cruzar_con = NULL,
  path_xlsx = "tablas_indices_spss.xlsx",
  hoja = "Indices",
  dic_vars = NULL,
  labels_override = NULL,
  nombres_indicadores = NULL,
  survey = NULL,
  orders_list = NULL,
  fuente = "Pulso PUCP",
  weight_col = NULL,
  digits = 1,
  incluir_total = TRUE,
  mostrar_todo = FALSE,
  agregar_brecha = FALSE,
  etiqueta_brecha = "Brecha"
)
}
\arguments{
\item{data}{\code{data.frame} con indicadores numéricos (0-100).}

\item{secciones}{Vector o lista nombrada de indicadores. Si es vector, se usa
una sección única definida por \code{titulo_default}. Se ignora cuando se usa
\code{tablas}.}

\item{tablas}{Lista opcional de tablas a exportar en un mismo Excel. Cada
elemento puede declarar: \code{titulo}, \code{indicadores}, \code{fila}, \code{cruzar_con},
\code{nombres_indicadores}, \code{agregar_brecha}, \code{etiqueta_brecha} e
\code{incluir_total}.}

\item{titulo_default}{Título de sección por defecto cuando \code{secciones} se pasa
como vector (no lista).}

\item{modo_cruces}{Cómo tratar múltiples cruces:
\code{juntos} (un solo bloque horizontal) o \code{separados} (una tabla por cruce).}

\item{separacion_filas}{Número de filas en blanco entre tablas dentro de la
misma hoja.}

\item{fila}{Variable opcional para filas (por ejemplo, \code{servicio} o
\code{municipio}). Si es \code{NULL}, se usa una sola fila \code{"Total"}.}

\item{cruzar_con}{Vector opcional de variables para columnas (una o varias).}

\item{path_xlsx}{Ruta de salida Excel.}

\item{hoja}{Nombre de hoja de salida.}

\item{dic_vars}{Diccionario opcional de etiquetas (\code{name}, \code{label}).}

\item{labels_override}{Lista opcional de etiquetas por variable.}

\item{nombres_indicadores}{Vector/lista nombrada para renombrar indicadores
en el encabezado de columnas (clave = nombre de variable, valor = etiqueta).}

\item{survey}{\code{survey} del instrumento (opcional).}

\item{orders_list}{\code{orders_list} del instrumento (opcional).}

\item{fuente}{Texto de fuente al pie de tabla.}

\item{weight_col}{Variable de peso. Si es \code{NULL}, intenta
\code{attr(data, "var_peso")} y luego \code{peso}.}

\item{digits}{Número de decimales para el promedio.}

\item{incluir_total}{Si \code{TRUE}, agrega bloque de columnas \code{"Total"} aun cuando
existan cruces.}

\item{mostrar_todo}{Si \code{TRUE}, muestra todas las categorías definidas en
instrumento para \code{fila}/\code{cruzar_con}, aunque no tengan casos.}

\item{agregar_brecha}{Si \code{TRUE}, agrega fila \code{"Brecha"} calculada como
\code{max - min} entre categorías de fila para cada columna de indicador.}

\item{etiqueta_brecha}{Texto de la etiqueta de la fila de brecha.}
}
\value{
Ruta normalizada del archivo Excel generado (invisible).
}
\description{
Genera tablas de promedios ponderados para variables recodificadas e índices
(sin desviación estándar ni percentiles), con estilo SPSS y encabezados
humanos mergeados. Permite:
\itemize{
\item Filas por una variable de agrupación (\code{fila}), o solo total.
\item Columnas por una o varias variables de cruce (\code{cruzar_con}).
\item Redondeo configurable con criterio escolar (half-up).
}
}
\details{
Estructura de columnas:
\itemize{
\item Nivel 1: variable de cruce (ej. Sexo, Distrito).
\item Nivel 2: categoría del cruce (ej. Mujer, Hombre).
\item Nivel 3: indicador (variables declaradas en \code{secciones}).
}
}
\seealso{
Other indicador: 
\code{\link{indicador}()},
\code{\link{nivel}()},
\code{\link{reporte_construir_indices}()},
\code{\link{reporte_dimensiones_config}()},
\code{\link{reporte_indicadores}()},
\code{\link{reporte_recodificar_items}()}
}
\concept{indicador}
