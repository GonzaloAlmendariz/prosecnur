% Generated by hand (compatible with roxygen style)
\name{reporte_tablas_recodificadas}
\alias{reporte_tablas_recodificadas}
\title{Exportar tablas SPSS de promedios (0-100) con cruces horizontales}
\usage{
reporte_tablas_recodificadas(
  data,
  secciones,
  tablas = NULL,
  titulo_default = "INDICES",
  modo_cruces = c("juntos", "separados"),
  separacion_filas = 3,
  fila = NULL,
  cruzar_con = NULL,
  path_xlsx = "tablas_indices_spss.xlsx",
  hoja = "Indices",
  dic_vars = NULL,
  labels_override = NULL,
  nombres_indicadores = NULL,
  survey = NULL,
  orders_list = NULL,
  fuente = "Pulso PUCP",
  weight_col = NULL,
  digits = 1,
  incluir_total = TRUE,
  mostrar_todo = FALSE,
  agregar_brecha = FALSE,
  etiqueta_brecha = "Brecha"
)
}
\arguments{
\item{data}{\code{data.frame} con indicadores numéricos (0-100).}

\item{secciones}{Vector o lista nombrada de indicadores. Si es vector, se usa
la sección única definida por \code{titulo_default}. Se ignora si se usa
\code{tablas}.}

\item{tablas}{Lista opcional de tablas para exportar en un solo Excel. Cada
elemento puede incluir: \code{titulo}, \code{indicadores}, \code{fila},
\code{cruzar_con}, \code{nombres_indicadores}, \code{agregar_brecha},
\code{etiqueta_brecha} e \code{incluir_total}.}

\item{titulo_default}{Título por defecto cuando \code{secciones} se pasa como
vector.}

\item{modo_cruces}{\code{"juntos"} para combinar cruces en el mismo bloque
horizontal; \code{"separados"} para crear una tabla por cada variable de cruce.}

\item{separacion_filas}{Número de filas en blanco entre tablas en la misma
hoja.}

\item{fila}{Variable opcional de filas. Si es \code{NULL}, se usa una sola
fila \code{"Total"}.}

\item{cruzar_con}{Vector opcional de variables de cruce para columnas.}

\item{path_xlsx}{Ruta de salida Excel.}

\item{hoja}{Nombre de hoja en Excel.}

\item{dic_vars}{Diccionario opcional de etiquetas con columnas
\code{name} y \code{label}.}

\item{labels_override}{Lista opcional de etiquetas por variable.}

\item{nombres_indicadores}{Vector/lista nombrada para renombrar indicadores
en encabezados (clave = variable, valor = etiqueta).}

\item{survey}{\code{survey} del instrumento (opcional).}

\item{orders_list}{\code{orders_list} del instrumento (opcional).}

\item{fuente}{Texto de fuente al pie de tabla.}

\item{weight_col}{Variable de peso. Si es \code{NULL}, intenta
\code{attr(data, "var_peso")} y luego \code{peso}.}

\item{digits}{Número de decimales para los promedios (redondeo escolar
half-up).}

\item{incluir_total}{Si es \code{TRUE}, agrega bloque de columnas
\code{"Total"} aun cuando existan cruces.}

\item{mostrar_todo}{Si es \code{TRUE}, muestra todas las categorías del
instrumento para \code{fila}/\code{cruzar_con}, aunque no tengan casos.}

\item{agregar_brecha}{Si es \code{TRUE}, agrega fila de brecha
(\code{max - min}) por indicador.}

\item{etiqueta_brecha}{Texto de la fila de brecha.}
}
\value{
Ruta normalizada del archivo Excel generado (invisible).
}
\description{
Genera tablas de promedios ponderados (sin desviación estándar ni percentiles),
con estilo SPSS, encabezados humanos mergeados y cruces horizontales.
}
