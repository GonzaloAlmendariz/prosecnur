% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_ppt.R
\name{reporte_ppt_plan}
\alias{reporte_ppt_plan}
\title{Reporte PowerPoint basado en "plan" (p_* + diapo_###)}
\usage{
reporte_ppt_plan(
  data,
  instrumento = NULL,
  path_ppt = "reporte_ppt_plan.pptx",
  presets = NULL,
  plan = NULL,
  env_diapos = parent.frame(),
  strict_diapos = FALSE,
  template_pptx = getOption("prosecnur.template_pptx", NA_character_),
  master = "Office Theme",
  mensajes_progreso = TRUE,
  solo_lista = FALSE
)
}
\arguments{
\item{data}{\code{data.frame} o \code{tibble} con las variables (o dummies) a reportar.}

\item{instrumento}{Objeto de instrumento con al menos \code{survey} (y opcionalmente \code{choices},
\code{orders_list}). Si es \code{NULL}, se busca el atributo \code{instrumento_reporte} en \code{data}.}

\item{path_ppt}{Ruta del \code{.pptx} de salida.}

\item{presets}{Objeto creado con \code{p_presets()}. Contiene estilos base por tipo de gráfico
(barras_agrupadas, barras_apiladas, multi_apiladas, pie, donut, numerico, texto/base, etc.)
y configuración de layouts/placeholders.}

\item{plan}{Lista de slides ya construidos con \code{p_plan()} o \code{list(diapo_001=..., ...)}.
Si es \code{NULL}, se recolectan objetos \code{diapo_###} desde \code{env_diapos}.}

\item{env_diapos}{Entorno donde se buscarán objetos \code{diapo_###} cuando \code{plan} sea \code{NULL}.
Por defecto se usa \code{parent.frame()}.}

\item{strict_diapos}{Si \code{TRUE}, errores en vez de warnings cuando los \code{diapo_###} no son
consecutivos o cuando hay inconsistencias estructurales (por ejemplo, slot requerido vacío).}

\item{template_pptx}{Ruta a plantilla \code{.pptx}. Si es \code{NULL}, se intenta usar una plantilla
interna del paquete y, si no existe, la plantilla por defecto de PowerPoint.}

\item{master}{Nombre del master de la plantilla (por defecto \code{"Office Theme"}).}

\item{mensajes_progreso}{Si \code{TRUE}, imprime mensajes de avance durante el proceso.}

\item{solo_lista}{Si \code{TRUE}, no se escribe el archivo y solo se retorna el objeto de salida.}
}
\value{
Invisiblemente una lista con:
\describe{
\item{doc}{Objeto \code{officer::pptx} (solo si \code{solo_lista = TRUE} o para depuración).}
\item{plan}{Plan normalizado de slides (lista).}
\item{log}{Tabla con decisiones/alertas por slide y por elemento.}
}
}
\description{
Genera un archivo \strong{.pptx} a partir de un \strong{plan de diapositivas} compuesto por:
\itemize{
\item \strong{elementos} \verb{p_*()} (gráficos / texto / base),
\item \strong{slides} \verb{p_slide_*()} (contenedores con layout fijo),
\item y objetos \code{diapo_###} (convención para recolección automática).
}

El flujo recomendado es:
\enumerate{
\item Definir un objeto de presets con \code{p_presets()} en un chunk previo.
\item Definir \code{diapo_001 <- p_slide_*(...)}, \code{diapo_002 <- ...} (uno o varios chunks).
\item Llamar a \code{reporte_ppt_plan(presets = presets, ...)} para recolectar y exportar.
}
}
