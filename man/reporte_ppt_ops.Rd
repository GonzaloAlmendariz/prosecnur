% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporte_ppt_ops.R
\name{reporte_ppt_ops}
\alias{reporte_ppt_ops}
\title{Generar un PPT de frecuencias simples usando la plantilla OPS}
\usage{
reporte_ppt_ops(
  data,
  instrumento = NULL,
  secciones = NULL,
  path_pptx = "reporte_ppt_ops.pptx",
  template_pptx = NULL,
  ...
)
}
\arguments{
\item{data}{\code{data.frame} o \code{tibble} con las variables (o sus dummies) a reportar.}

\item{instrumento}{Objeto de instrumento con al menos \code{survey} (y opcionalmente \code{choices}
y \code{orders_list}). Si es \code{NULL}, se busca el atributo \code{instrumento_reporte} en \code{data}.}

\item{secciones}{Lista nombrada donde cada elemento contiene un vector de nombres de variables
a incluir por sección. Si es \code{NULL}, se intenta inferir desde \code{survey$section} o \code{survey$seccion}.}

\item{path_pptx}{Ruta del archivo PPTX a generar. Por defecto
\code{"reporte_ppt_ops.pptx"}.}

\item{template_pptx}{Ruta a una plantilla PPTX. Si es \code{NULL}, se busca
una plantilla interna llamada \code{"plantillas/plantilla_OPS.pptx"} dentro
del paquete.}
}
\value{
Lo mismo que devuelve \code{reporte_ppt()}: típicamente una lista con
gráficos y el log de decisiones, de forma invisible si solo se genera
el archivo PPTX.
}
\description{
\code{reporte_ppt_ops()} es un envoltorio ligero sobre \code{\link[=reporte_ppt]{reporte_ppt()}} que
utiliza por defecto una plantilla de PowerPoint específica para OPS
(\code{plantilla_OPS.pptx}), manteniendo la misma lógica de graficación y
recorrido por secciones que el reporte estándar de frecuencias simples.
}
\details{
En la práctica:
\itemize{
\item Llama internamente a \code{reporte_ppt()}.
\item Si \code{template_pptx} es \code{NULL}, busca el archivo
\code{"plantillas/plantilla_OPS.pptx"} dentro del paquete
(carpeta \code{inst/plantillas/}).
\item Si se proporciona \code{template_pptx}, usa esa ruta en su lugar.
}

Esto permite mantener intacta la implementación de \code{reporte_ppt()} y, a la vez,
trabajar con una plantilla específica para un estudio (OPS) o para un cliente.
}
