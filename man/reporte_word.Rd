% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporte_word_generador.R
\name{reporte_word}
\alias{reporte_word}
\title{Generar un reporte Word a partir de frecuencias (SO/SM) con graficadores propios}
\usage{
reporte_word(
  data,
  instrumento = NULL,
  secciones = NULL,
  path_word = "reporte_word.docx",
  fuente = NULL,
  sm_vars_force = NULL,
  mostrar_todo = FALSE,
  solo_lista = FALSE,
  incluir_titulo_var = TRUE,
  mensajes_progreso = TRUE,
  template_docx = NULL,
  bloques_multi_apiladas = NULL,
  vars_dico = NULL,
  vars_barras_apiladas = NULL,
  vars_barras_agrupadas = NULL,
  vars_radar = NULL,
  listnames_dico = NULL,
  listnames_apiladas = NULL,
  dico_labels_por_var = list(),
  dico_labels_por_listname = list(),
  colores_apiladas_por_listname = list(),
  default_so = c("barras_apiladas", "barras_agrupadas"),
  default_sm = c("barras_agrupadas", "barras_apiladas"),
  barra_extra = c("ninguna", "total_n"),
  estilos_barras_agrupadas = list(),
  estilos_barras_apiladas = list(),
  estilos_dico = list()
)
}
\arguments{
\item{data}{\code{data.frame} o \code{tibble} con las variables (o sus dummies) a reportar.}

\item{instrumento}{Objeto de instrumento con al menos \code{survey} (y opcionalmente \code{choices}
y \code{orders_list}). Si es \code{NULL}, se busca el atributo \code{instrumento_reporte} en \code{data}.}

\item{secciones}{Lista nombrada donde cada elemento contiene un vector de nombres de variables
a incluir por sección. Si es \code{NULL}, se intenta inferir desde \code{survey$section} o \code{survey$seccion}.}

\item{path_word}{Ruta del archivo DOCX a generar cuando \code{solo_lista = FALSE}.}

\item{fuente}{Texto de fuente que se mostrará debajo de cada gráfico
en el documento Word (por ejemplo, \code{" estudiantes. Fuente: Pulso PUCP 2025."},
que se concatenará luego de \code{"N = <N> "}).}

\item{sm_vars_force}{Vector de variables que deben tratarse como selección múltiple (si aplica
en la lógica de clasificación).}

\item{mostrar_todo}{Si \code{TRUE}, conserva opciones con frecuencia cero al construir tablas (si la
tabla de frecuencias lo permite).}

\item{solo_lista}{Si \code{TRUE}, no se genera el PowerPoint. Se devuelven gráficos y el log.}

\item{incluir_titulo_var}{Si \code{TRUE}, usa el título de la pregunta como título de la diapositiva.}

\item{mensajes_progreso}{Si \code{TRUE}, imprime mensajes de avance durante el proceso.}

\item{template_docx}{Ruta a una plantilla DOCX. Si es \code{NULL}, se intenta
usar una plantilla interna \code{"plantillas/plantilla_pulso.docx"} y, si
no existe, se usa la plantilla por defecto de Word.}

\item{bloques_multi_apiladas}{Lista nombrada de bloques especiales para
multi-apiladas. Cada elemento debe ser una lista con al menos
\code{vars = c("p101","p102",...)} y opcionalmente:
\code{titulo}, \code{wrap_y}, \code{grosor_barras}, \code{invertir_barras}.}

\item{vars_dico, vars_barras_apiladas, vars_barras_agrupadas, vars_radar}{Vectores de nombres de
variables que fuerzan el tipo de gráfico por variable.}

\item{listnames_dico, listnames_apiladas}{Vectores de \code{list_name} que fuerzan el tipo de gráfico
por lista.}

\item{dico_labels_por_var}{Lista con etiquetas dicotómicas por variable. Cada elemento debe
ser un vector de dos etiquetas (por ejemplo, \code{c("Sí", "No")}).}

\item{dico_labels_por_listname}{Lista con etiquetas dicotómicas por \code{list_name}, con el mismo
formato que \code{dico_labels_por_var}.}

\item{colores_apiladas_por_listname}{Lista con definiciones de colores para barras apiladas
por \code{list_name}. Puede incluir un vector nombrado de colores y, opcionalmente, parámetros
extra asociados al estilo de barra adicional.}

\item{default_so}{Tipo de gráfico por defecto para variables de opción única (por ejemplo,
\code{"barras_apiladas"} o \code{"barras_agrupadas"}).}

\item{default_sm}{Tipo de gráfico por defecto para variables de selección múltiple.}

\item{barra_extra}{Define el tratamiento de una anotación adicional en gráficos de barras:
\code{"ninguna"} o \code{"total_n"}.}

\item{estilos_barras_agrupadas}{Lista de parámetros de estilo que se pasan al graficador de
barras agrupadas.}

\item{estilos_barras_apiladas}{Lista de parámetros de estilo que se pasan al graficador de
barras apiladas.}

\item{estilos_dico}{Lista de parámetros de estilo que se pasan al graficador dicotómico.}
}
\value{
Una lista con:
\describe{
\item{plots}{Lista de objetos \code{ggplot} generados (uno por variable o bloque).}
\item{log_decisiones}{Tibble con información sobre cada variable procesada.}
}
}
\description{
\code{reporte_word()} toma una base de reporte (idealmente la misma que se usa en
\code{reporte_frecuencias()}), el instrumento y una definición de secciones, y
genera:
\itemize{
\item Una lista de gráficos \code{ggplot} (uno por variable válida o bloque).
\item Opcionalmente, un archivo Word donde cada gráfico ocupa un bloque:
título externo + gráfico centrado + pie azul con fuente.
}
}
\details{
El tipo de gráfico para cada variable se decide igual que en \code{reporte_ppt()}:
\enumerate{
\item Sobrescritura explícita por variable:
\itemize{
\item \code{vars_dico}: variables que deben usarse como dicotómicas
con \code{graficar_dico()}.
\item \code{vars_barras_apiladas}: variables para
\code{graficar_barras_apiladas()}.
\item \code{vars_barras_agrupadas}: variables para
\code{graficar_barras_agrupadas()}.
\item \code{vars_radar}: reservado para futuros usos (no se construye
tabla radar automáticamente).
}
\item Decisión por \code{list_name} de la pregunta en el instrumento:
\itemize{
\item \code{listnames_dico}: todas las variables cuyo \code{list_name}
esté aquí tienden a graficarse como dicotómicas.
\item \code{listnames_apiladas}: todas las variables cuyo \code{list_name}
esté aquí tienden a graficarse como barras apiladas.
}
\item Defaults por tipo de pregunta:
\itemize{
\item \code{default_so}: tipo de gráfico por defecto para \code{select_one}.
\item \code{default_sm}: tipo de gráfico por defecto para
\code{select_multiple}.
}
}

Para las variables dicotómicas, no se asume ninguna pareja de categorías por
defecto: se requiere definirla en \code{dico_labels_por_var} o
\code{dico_labels_por_listname}. Si no se encuentra pareja válida, se usa
barras agrupadas por defecto.

En el documento Word:
\itemize{
\item El título del gráfico se escribe \emph{fuera} del gráfico, como
\code{"Gráfico Nº X: "} (en negrita y cursiva) seguido del label de la
variable, todo en color azul Pulso.
\item El gráfico se inserta centrado, sin título interno.
\item Debajo del gráfico se escribe un pie centrado en azul con el texto
\code{"N = ..."} seguido de \code{fuente} cuando se indique.
\item Dentro del gráfico, en el caption derecho, se escribe el \code{N}
usando los argumentos \code{nota_pie_derecha} y \code{pos_nota_pie}
de los graficadores.
}
}
\seealso{
Other reporte: 
\code{\link{diapo}()},
\code{\link{exportar_frecuencias_spss}()},
\code{\link{freq_table_spss}()},
\code{\link{generar_spss_niveles}()},
\code{\link{p_barras_agrupadas}()},
\code{\link{p_barras_apiladas}()},
\code{\link{p_barras_multiapiladas}()},
\code{\link{p_dim_heatmap}()},
\code{\link{p_dim_radar}()},
\code{\link{p_donut}()},
\code{\link{p_get_plan}()},
\code{\link{p_numerico}()},
\code{\link{p_pie}()},
\code{\link{p_plan}()},
\code{\link{p_presets}()},
\code{\link{p_radar_tabla}()},
\code{\link{p_slide_1}()},
\code{\link{p_slide_2}()},
\code{\link{p_slide_section}()},
\code{\link{p_slide_title}()},
\code{\link{p_text}()},
\code{\link{plan_word}()},
\code{\link{reporte_codebook}()},
\code{\link{reporte_cruces}()},
\code{\link{reporte_data}()},
\code{\link{reporte_enumeradores}()},
\code{\link{reporte_frecuencias}()},
\code{\link{reporte_instrumento}()},
\code{\link{reporte_ppt}()},
\code{\link{reporte_ppt_cruces}()},
\code{\link{reporte_ppt_ops}()},
\code{\link{reporte_ppt_plan}()},
\code{\link{reporte_spss}()},
\code{\link{reporte_word_cruces}()},
\code{\link{w_presets}()}
}
\concept{reporte}
